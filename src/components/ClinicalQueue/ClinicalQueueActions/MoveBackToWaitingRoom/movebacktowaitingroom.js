import React from "react";
import Typography from "@material-ui/core/Typography";

export default function MoveBackToWaitingRoom(props) {
    return (
        <div>
            <Typography>Move Back To Waiting Room</Typography>
        </div>
    );
}